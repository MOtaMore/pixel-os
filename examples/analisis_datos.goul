// 📊 Análisis de Datos Simple en Goul

print("=== 📊 Análisis de Datos ===\n");

// Dataset: Ventas semanales
var ventas = [120, 150, 180, 140, 200, 175, 190];
var dias = ["Lun", "Mar", "Mié", "Jue", "Vie", "Sáb", "Dom"];

print("--- Datos de la Semana ---");
print("Total de días: " + str(len(ventas)));

// Mostrar ventas por día
print("\nVentas diarias:");
print(dias[0] + ": $" + str(ventas[0]));
print(dias[1] + ": $" + str(ventas[1]));
print(dias[2] + ": $" + str(ventas[2]));
print(dias[3] + ": $" + str(ventas[3]));
print(dias[4] + ": $" + str(ventas[4]));
print(dias[5] + ": $" + str(ventas[5]));
print(dias[6] + ": $" + str(ventas[6]));

// Calcular total
var total = ventas[0] + ventas[1] + ventas[2] + ventas[3] + ventas[4] + ventas[5] + ventas[6];
print("\n--- Estadísticas ---");
print("Total de ventas: $" + str(total));

// Calcular promedio
var promedio = total / len(ventas);
print("Promedio diario: $" + str(promedio));

// Encontrar mejor día
var mejor_venta = ventas[4];  // Viernes
var mejor_dia = dias[4];
print("Mejor día: " + mejor_dia + " con $" + str(mejor_venta));

// Encontrar peor día
var peor_venta = ventas[0];  // Lunes
var peor_dia = dias[0];
print("Peor día: " + peor_dia + " con $" + str(peor_venta));

// Calcular diferencia
var diferencia = mejor_venta - peor_venta;
print("Diferencia: $" + str(diferencia));

// Proyección semanal
var proyeccion_mensual = total * 4;
print("\n--- Proyecciones ---");
print("Proyección mensual: $" + str(proyeccion_mensual));

// Metas
var meta_semanal = 1200;
if (total >= meta_semanal) {
    print("✅ Meta semanal alcanzada!");
} else {
    var faltante = meta_semanal - total;
    print("❌ Falta: $" + str(faltante));
}

print("\n📈 Análisis completado!");
